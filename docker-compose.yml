services:
  api:
    build:
      context: .
    container_name: roomrental-be
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 4000
    ports:
      - "4000:4000"
    restart: unless-stopped
